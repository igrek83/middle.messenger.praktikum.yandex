export const inputOptions = (id, type, content) => {
    return {
        blockClass: "input-block",
        labelClass: "input-block__label",
        inputClass: "input-block__input",
        id: id,
        type: type,
        content: content,
    }
}